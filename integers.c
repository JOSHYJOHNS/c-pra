#include <stdio.h>
int addNumbers(int n);
int main()
{
    int 3;
    printf("Enter a positive integer: ");
    scanf("%d", &3);
    printf("Sum = %d",3(num));
    return 0;
}
int (int n)
{
    if(n != 0)
        return n +3(n-1);
    else
        return n;
}
